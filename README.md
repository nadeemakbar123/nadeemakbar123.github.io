# StackEnterprises.dev Landing Page

This repository contains a Next.js landing page prepared for GitHub Pages static export.

## Development

```bash
npm install
npm run dev
```

## Build for GitHub Pages

```bash
npm run build
```

The static files are generated in `out/` and `CNAME` is copied into the export output by `postbuild`.